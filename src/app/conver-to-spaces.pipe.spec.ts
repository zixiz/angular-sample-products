import { ConverToSpacesPipe } from './conver-to-spaces.pipe';

describe('ConverToSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverToSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
